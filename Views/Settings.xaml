<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BouncyCat.Views.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BouncyCat.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tool="using:CommunityToolkit.WinUI.Controls"
    xmlns:ic="using:FluentIcons.WinUI"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="60"></RowDefinition>
            <RowDefinition Height="12*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock FontSize="24" Text="设定" FontWeight="Bold" Foreground="#4d9891" Grid.Row="0" HorizontalAlignment="Left" Margin="15,10,10,10" ></TextBlock>
        <Border BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1" Height="1" Grid.Row="0" VerticalAlignment="Bottom"></Border>
        <ScrollViewer Grid.Row="1" Padding="20" Margin="0,0,0,15">
            <StackPanel Orientation="Vertical" Spacing="10">
                <TextBlock  Text="调试" FontSize="16" Margin="5,2,0,5"></TextBlock>
                <tool:SettingsExpander Header="数据库位置">
                    <tool:SettingsExpander.HeaderIcon>
                        <ic:SymbolIcon Symbol="FolderAdd"></ic:SymbolIcon>
                    </tool:SettingsExpander.HeaderIcon>
                    <tool:SettingsExpander.Items>
                        <TextBlock x:Name="de" Text="" IsTextSelectionEnabled="True"></TextBlock>
                    </tool:SettingsExpander.Items>
                    
                </tool:SettingsExpander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
